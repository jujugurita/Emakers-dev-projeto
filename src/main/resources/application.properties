spring.application.name=biblioteca

# Configurações do MySQL adaptadas para o contêiner do Docker
spring.datasource.url=jdbc:mysql://db-biblioteca:3306/biblioteca?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=158273
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Ajuste do Hibernate para não conflitar com o Flyway (Ganhar 1 ponto)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração extra do Flyway para MySQL
spring.flyway.baseline-on-migrate=true